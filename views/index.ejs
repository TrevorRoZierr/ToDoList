<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <div class="title">
        <h1>GROVE</h1>
        <h3>To-Do List</h3>
        <p><%= day %></p>
    </div>

    <div class="container">
        <div class="listMain">
            <ol>
                <% for(var i = 0; i < inputData.length; i++) { %>
                    <li><%= inputData[i] %></li>
                <% } %>
            </ol>
        </div>
        <div class="formSubmit">
            <form action="/" method="post">
                <input type="text" name="liInput" placeholder="Add in to-do list..." autocomplete="off">
                <button type="submit">+</button>
            </form>
        </div>

        <div class="resetBtn">
            <h4 id="checkBtn">Check</h4>
        </div>
    </div>

    <footer class="footer">
        <p>Owned By Sayyed Saad<sup>©</sup></p>
    </footer>

    <script src="javascript/script.js"></script>
</body>
</html>